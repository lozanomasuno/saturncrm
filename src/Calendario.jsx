import { BrowserRouter } from 'react-router-dom';
import { AppRouter } from './router';

export const Calendario = () => {
  return (
    <BrowserRouter>
     <AppRouter />
    </BrowserRouter>
  )
}
